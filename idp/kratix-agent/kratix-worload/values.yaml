# Values to configure the resources:
# Application CR - kratix dependencies
# Application CR - kratix resources
# Secret         - Gitea server credentials

clusterName: worker1
giteaServer:
  url: http://<HOST_IP_ADDRESS>:<NODE_PORT>
  username: "giteaAdmin"
  password: "developer"

gitKratix:
  org: kratix
  repository: state